import { FloatPricePipe } from './float-price.pipe';

describe('FloatPricePipe', () => {
  it('create an instance', () => {
    const pipe = new FloatPricePipe();
    expect(pipe).toBeTruthy();
  });
});
